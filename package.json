{
  "name": "fluent-kit",
  "version": "0.1.0",
  "main": "index.js",
  "repository": "git@github.com:thure/fluent-kit.git",
  "author": "Will Shown <willshown@microsoft.com>",
  "license": "MIT",
  "private": true,
  "types": "types/view.d.ts",
  "scripts": {
    "lint": "prettier --write src/**/*.json",
    "build": "rm -rf types && json2ts -i 'src/*.json' -o types/ --cwd src",
    "prepare": "husky install"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "devDependencies": {
    "husky": ">=6",
    "json-schema-to-typescript": "^10.1.4",
    "lint-staged": ">=10",
    "prettier": "^2.3.2"
  },
  "lint-staged": {
    "*.{json}": "prettier --write"
  }
}
