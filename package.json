{
  "name": "fluent-kit",
  "version": "0.1.0",
  "main": "index.js",
  "repository": "git@github.com:thure/fluent-kit.git",
  "author": "Will Shown <willshown@microsoft.com>",
  "license": "MIT",
  "private": true,
  "types": "types/index.d.ts",
  "scripts": {
    "build": "rm -rf types && json2ts -i 'src/*.json' -o types/ --cwd src"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^10.1.4"
  }
}
