import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { Illustration } from './StorybookIllustration'
import { themeArgType } from '../../lib'

<Meta
  title="Media/Illustrations"
  component={Illustration}
  parameters={{ viewMode: 'docs' }}
  argTypes={{ ...themeArgType, illustration: { table: { disable: true } } }}
/>

export const IllustrationTemplate = (props) => <Illustration {...props} />

# Illustration

This library comes with a set of built-in illustrations you can simply reference by name in a `Figure` or `BigMessage` component.

### Default:

<Canvas withSource="none">
  <Story name="Default" args={{ theme: 'light', illustration: 'default' }}>
    {IllustrationTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

### Empty:

<Canvas withSource="none">
  <Story name="Empty" args={{ theme: 'light', illustration: 'empty' }}>
    {IllustrationTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Empty" />

### Error:

<Canvas withSource="none">
  <Story name="Error" args={{ theme: 'light', illustration: 'error' }}>
    {IllustrationTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Error" />

### Hello:

<Canvas withSource="none">
  <Story name="Hello" args={{ theme: 'light', illustration: 'hello' }}>
    {IllustrationTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Hello" />

### Thanks:

<Canvas withSource="none">
  <Story name="Thanks" args={{ theme: 'light', illustration: 'thanks' }}>
    {IllustrationTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Thanks" />
