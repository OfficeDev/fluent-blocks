import { fake, seed } from 'faker/locale/en_US'
import range from 'lodash/range'

import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import { contextArgTypes } from '../../lib'
import fakeTitle from '../../lib/fakeTitle'
import iconSpriteUrl from '../../lib/storybookIconSpriteUrl'
import { Tabs } from './StorybookTabs'

<Meta
  title="Blocks/Tabs"
  component={Tabs}
  argTypes={{
    ...contextArgTypes,
  }}
/>

export const TabsTemplate = (props) => (
  <Tabs {...props} iconSpriteUrl={iconSpriteUrl} />
)

# Tabs

⚠️ This component is an ersatz implementation pending the release of a more
canonical implementation by `@fluentui/react-components`.

<Canvas withSource="none">
  <Story
    name="Tabs"
    args={{
      themeName: 'light',
      accentScheme: 'teams',
      tabs: range(4).map(() => ({
        tab: { label: fakeTitle(fake) },
        panel: [{ paragraph: [{ text: fake('{{lorem.paragraph}}') }] }],
      })),
      label: fakeTitle(fake),
    }}
  >
    {seed(1234) || TabsTemplate.bind({})}
  </Story>
</Canvas>
