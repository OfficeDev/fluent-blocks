type IconVariant = IconVariant;

type IconSize = IconSize;

type IconProps = IconProps;

type TextProps = TextProps;

type InlineEntity = InlineEntity;

type InlineSequence = InlineSequence;

type InlineSequenceOrString = string | InlineSequence;

type InlineContentProps = {
    inlines: string | InlineSequence;
};

