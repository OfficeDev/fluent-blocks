import { MenuItemSequence } from '../lib/menu'
import { IconSize } from '../inlines'
import { ButtonSize } from './Button'

export interface OverflowProps {
  overflow: MenuItemSequence
  iconSize?: IconSize
  buttonSize?: ButtonSize
}
