import { fake, seed } from 'faker/locale/en_US'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'

import { Card } from './StorybookCard'
import { themeArgType } from '../../lib'

<Meta
  title="Blocks/Card (as a Block)"
  component={Card}
  argTypes={{
    ...themeArgType,
    card: {
      name: 'Card',
      control: {
        type: 'object',
      },
      table: {
        type: {
          summary: 'CardContentItemSequence',
        },
      },
    },
  }}
  parameters={{
    viewMode: 'docs',
  }}
/>

export const CardTemplate = (props) => <Card {...props} />

# Card (as a Block)

While `Card` elements are most often used as content items of a `Layout`
element, the pattern can be used to elevate and encapsulate content as a
regular Block as well.

<Canvas withSource="none">
  <Story
    name="Card (as a Block)"
    args={{
      theme: 'light',
      card: [
        {
          paragraph: [{ text: 'This is a Card as a regular Block element.' }],
          level: 1,
        },
        { paragraph: [{ text: fake('{{lorem.paragraph}}') }] },
      ],
    }}
  >
    {seed(1234) || CardTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Card (as a Block)" />
