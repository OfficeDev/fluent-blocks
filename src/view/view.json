{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "View",
  "description": "An experience provided to the user via their device’s canvas.",
  "type": "object",
  "required": ["main"],
  "properties": {
    "sidebar": { "$ref": "../surfaces/sidebar.json" },
    "toolbar": { "$ref": "../surfaces/toolbar.json" },
    "modal": { "$ref": "../surfaces/modal.json" },
    "main": { "$ref": "../blocks/section/section.json" },
    "theme": { "enum": ["light", "dark", "high-contrast"] },
    "dir": { "enum": ["ltr", "rtl"] }
  },
  "additionalProperties": false
}
